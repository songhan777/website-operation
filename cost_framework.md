# AI 网站全生命周期成本：独立审查框架

编制日期：2026-09-13。供主文档整合使用。除文末明确引用的供应商计费机制外，本文中的人力价格、百分比目标、调用单价和业务案例均为**规划假设或数学演示，不是市场报价、供应商报价或保证节省比例**。

## 1. 先定义网站和核算口径

基准适用于已有大模型 API 支持的文本生成、知识问答、文档处理网站，包含登录、支付、用量统计、管理后台、简单 RAG。训练基础模型不属于该基准范围。图片、音频、视频、复杂 Agent 应按各自用量另外建模。

- **现金支出**：当前月份实际要付的钱，用于预算和现金流；预付年费要按真实付款月份体现。
- **服务交付成本**：为交付服务发生的模型、云资源、存储、网络、支付与交付相关客服等费用，用于管理口径毛利；固定交付人力和基础设施也要纳入。
- **完整经营成本**：交付成本加产品研发、行政、营销、负责人薪酬等；创始人未领薪酬也应单列机会成本。
- **一次性建设投入**：开发、设计、数据整理、迁移和上线准备；可另列 12 或 24 个月的管理摊销用于比较方案，但不能既将全部建设投入计入当年支出，又重复加一遍同一笔管理摊销。管理摊销不是会计或税务处理建议。
- **收入与收款分开**：套餐标价、扣除折扣退款后的收入、到账金额不同；支付手续费只能计一次。

同一套报表至少展示「每月现金支出」「月度服务毛利」「月度经营盈亏」「首年总投入」四项，避免只给一个看起来很低的 API 账单。

## 2. 一次性投入与人力预算

以下是三种**建模区间**，应以功能清单、验收标准和真实报价替换。每人月单价为预算里的综合人力成本参数，不是薪资调查；不同城市、外包范围与团队效率可明显偏离。

| 方案 | 典型范围 | 假设投入 | 假设综合单价 | 推导出的建设人力成本 |
|---|---|---:|---:|---:|
| 验证型 MVP | 单一核心工作流、模板界面、基本账号/额度、少量支付和后台功能 | 2–4 人月 | 2–4 万元/人月 | 4–16 万元 |
| 可收费商业版 | 文档处理/RAG、订阅与账单、管理后台、埋点、质量评估、异常恢复 | 5–10 人月 | 3–5 万元/人月 | 15–50 万元 |
| 企业或复杂产品 | 多租户、权限/审计、多个系统集成、复杂 Agent、较高可用性与交付要求 | 12–30 人月 | 4–7 万元/人月 | 48–210 万元 |

人月不是项目工期：5 人月可能由两个人在约 2.5–4 个月完成，也可能因需求与依赖拉长。AI 编程工具可以减少部分编码时间，但不能直接把需求分析、验证、权限隔离、支付对账、数据安全和维护的工作量归零。

建设预算还要单列：品牌与设计素材、域名与商标/法律文件准备、首批数据清洗与标注、知识库首次导入、内容版权或授权、测试账号与评估调用、迁移、第三方安全评估（若需要）以及不可预见工作。是否发生取决于项目，不应全部默认购买。

持续人力需要分开估算：技术维护与值班、模型效果与知识库更新、客服/退款/审核、产品迭代、增长运营。一个人在多个岗位兼职可以合并预算；不要把同一人的全部月薪重复算进每一栏。创始人兼职维护也有工作时间成本，且人员离开或生病时有替补成本。

## 3. 成本清单与容易漏项的驱动因素

| 成本域 | 主要项目 | 最容易漏掉的驱动因素 |
|---|---|---|
| 模型推理 | 输入、缓存输入/写入、输出、推理类计费、模型路由 | 历史消息反复输入；工具返回内容；隐藏的内部多轮；失败重试；用户取消后的继续生成 |
| RAG 与文档 | 解析/OCR、Embedding、向量存储、检索、重排、索引 | 同一文件重复导入；更新全量重建；扫描 PDF 和表格的解析；备份/副本；删除后的索引同步 |
| Agent | 规划、工具调用、联网检索、代码沙箱、浏览器、结果验证 | 单次用户任务变成多次模型/工具调用；循环；网页截图；沙箱会话时间；失败任务也已经消耗预算 |
| 多媒体 | 图片输入/输出、语音识别/合成、视频生成与处理 | 分辨率、时长、帧数、候选版本数、用户重生成；素材保存与下载流量；等待队列 |
| 计算与网络 | 前端/CDN、应用服务、队列、负载均衡、GPU | 按请求和运行时长同时计费；流式长连接；冷启动；跨区传输；NAT；固定 IP；出网 |
| 数据平台 | 数据库、对象存储、缓存、搜索、备份 | IOPS/操作请求费；连接数；向量和索引膨胀；复制；数据库恢复测试；非生产环境 |
| 运营基础服务 | 登录认证、邮件、短信、验证码、日志/APM、客服工单 | MAU 与短信条数；异常注册；日志索引和保留；团队席位；告警与审计存储 |
| 收款与商业 | 支付手续费、固定交易费、退款/争议、汇兑、销售佣金 | 小额交易固定手续费占比高；年费退款；渠道抽成；供应商和客户使用不同币种 |
| 安全与治理 | WAF、反滥用、权限隔离、漏洞修复、数据清理 | 被盗 API Key；免费账户刷量；任意大文件/长上下文；人工审核；安全事件处置 |
| 研发及获客 | 工具订阅、CI/CD、测试、内容/广告、销售、人员 | 研发试验的 API 费用；预览环境不关闭；获客失败；客户流失；沉没研发工作 |

站点面向的地区、主体、数据类别与部署地区决定备案、隐私、内容治理、数据跨境等工作是否适用；在完整成本清单里应预留核查项，而不是假定所有 AI 网站承担同一种手续。

## 4. 必须按「成功完成的业务任务」核算

定义用户分组 g（免费、基础付费、高级付费、企业等）和任务类型 j（问答、长文档、图片、Agent 等）：

`月度任务数 Q_gj = 该组 MAU × 月活中使用 AI 的比例 × 每位使用者月任务数 × 类型占比`

`月度 AI 成本 = Σ_gj Q_gj × E[单次业务任务的全部模型与工具成本]`

任务内成本必须把每个模型调用相加：普通输入 token × 单价、缓存读/写 token × 对应单价、输出 token × 单价，加模型工具、第三方工具、OCR、沙箱与媒体费用。免费任务也要统计。仅拿「用户问题长度」估算输入量，会漏掉系统提示、历史消息、检索片段和工具响应。

把重试计入全部调用日志后，**不要再额外乘一次重试系数**。如果只从首次调用估算，才可以乘根据真实观测得到的平均调用放大系数。失败和取消任务的支出仍进入分子：

`每个成功任务的交付成本 = 当期相关全部交付成本 / 成功任务数`

例如同样花 100 元，方案 A 完成 100 个任务、方案 B 完成 150 个，A 的单调用价格可能更低，但成功任务成本可能更高。模型切换应以相同业务任务、相同质量与延迟要求进行比较。

月度 token 数决定账单，峰值并发与延迟要求决定容量，两者不能互相替代。容量至少需要估算 `峰值每秒任务数 × 每任务平均处理秒数` 得到在途请求数量，并检查模型供应商的请求/token 速率限制。长文档导入、发布活动与批量生成应做独立峰值场景。

## 5. 免费用户、定价与盈亏平衡

用下列管理口径能直接暴露免费用户补贴：

`月贡献利润 = 付费人数 × (付费用户净收入 − 其变量交付成本) − 免费人数 × 免费用户变量成本`

`月经营利润 = 月贡献利润 − 固定交付成本 − 固定研发/运营/行政成本 − 当期获客费用`

若免费人数固定，`盈亏平衡付费人数 = (全部固定成本 + 免费用户变量成本总额) / 每付费用户贡献额`，前提是贡献额大于零。若付费转化率 p 随总 MAU 建模，应使用：

`每 MAU 贡献 = p × 付费用户贡献额 − (1−p) × 免费用户变量成本`

`盈亏平衡 MAU = 固定成本 / 每 MAU 贡献`。若分母小于或等于零，扩大流量只会扩大损失。

**纯假设示例**：10,000 MAU、付费率 5%、每付费用户净收入 59 元、付费用户变量成本 15 元、免费用户变量成本 1 元、固定成本 30,000 元。付费贡献 `500×44=22,000` 元，免费补贴 `9,500×1=9,500` 元，经营结果为 `−17,500` 元。保持 5% 付费率与同等使用结构时，每 MAU 贡献为 1.25 元，需约 24,000 MAU 才覆盖 30,000 元固定成本；这尚未假设获客支出随增长增加，因此不能解读为确定盈利规模。

免费计划应按每人每日/每月的成本预算设计：低价模型、合理上下文/文件大小上限、任务次数、并发、有效期、昂贵工具权限和防批量注册。高成本功能宜采用积分/按次或超额计费，积分应对应各任务的预期成本，不宜把一张图片和一次普通问答都算成同一「次数」。用户可见的额度与失败退款规则要清楚。

年付可以改善现金流，但不是毛利优化；预收一年款不代表一年服务成本已经消失。大客户承诺额度、折扣和 SLA 也需要把专属支持、最低资源与违约服务补偿纳入测算。

新增客户经济性另外计算 `CAC = 可归因获客支出 / 新增付费客户数`；`回本月数 = CAC / 新客户月贡献`。历史短、留存不稳定时，应先展示按真实 cohort 得到的 3/6/12 个月累计贡献，不宜用未经验证的长期 LTV 放大盈利预期。

## 6. 降本顺序与质量保护

| 优先级 | 动作 | 为什么优先 | 验收与风险控制 |
|---|---|---|---|
| P0 | 服务端预算预占、额度结算、并发/速率限制、请求大小上限、Key 隔离与反滥用 | 先控制失控支出与刷量 | 按账户/租户/任务/全站四层限额；模型、工具和沙箱均有最大预算；并发压测不会超扣 |
| P0 | 记录任务级 token、工具调用、重试、取消、成本与结果 | 没有分项账本就无法知道钱花在哪里 | 每日与供应商账单核对；标记缓存读写；支持按模型、功能、套餐和租户查看 |
| P1 | 取消后停止生成、去重、幂等、有限重试、工具循环上限 | 通常能直接去掉没有用户价值的工作 | 避免重复扣费/重复调用；区分可重试与不可重试错误；验证断开后上游请求确实终止 |
| P1 | 按任务难度路由模型；简单分类/提取优先小模型或规则 | 模型单价与不必要调用是主要可控量 | 保留复杂任务回退；固定评估集比较成功率与成本；计入路由和升级的双次调用 |
| P1 | 限制无用长输出、结构化结果、历史摘要、RAG 精选片段 | 输出和反复输入的上下文均影响成本 | 按任务类型设长度；测关键事实遗漏、引用质量和业务验收率；摘要本身也有费用 |
| P1/P2 | 稳定公共前缀使用供应商 prompt cache；高重复内容使用结果缓存 | 分别减少相同输入处理或整个重复任务 | 缓存写入/读出/过期都纳入账本；结果缓存含租户/权限/数据版本，防泄漏和陈旧答案 |
| P2 | 离线任务批处理/队列、错峰、批量 Embedding、增量索引 | 以可接受的等待换成本与容量 | 只对允许延迟的任务使用；检查供应商折扣与 SLA；避免超过有效期的结果 |
| P2 | 同区域数据路径、存储生命周期、控制日志保留、关闭闲置环境 | 直接减少网络、存储与固定资源浪费 | 先定位实际账单；保留恢复与审计所需数据；恢复演练通过后调整备份 |
| P3 | 承诺用量、供应商议价、自部署或混合部署 | 收益依赖稳定负载和真实规模 | 有足够长期需求证据；包含运维、冗余、迁移与锁定成本；不用试用赠额判断长期经济性 |

建议先覆盖过去 30 天累计成本最高的任务类型，按 `预计月节省 / 一次性实施成本` 与回收周期排序。节省金额以真实分项账单为基准；低流量阶段，省几百元服务器却增加数万元维护开发工作通常不划算。

**可组合但不能直接相加的演示**：假设某任务 4,000 输入、1,000 输出 token，输入单价 1 美元/百万、输出单价 4 美元/百万（均为虚构计算参数），原价 0.008 美元。输入减半后为 0.006 美元，节省 25%；只把输出减至 600 token 为 0.0064 美元，节省 20%；同时实施为 0.0044 美元，节省 45%。若再假设压缩后的 2,000 输入中 60% 按原价 10% 读取缓存，且忽略写入费用，则成本为 0.00332 美元，较原来节省 58.5%。这里的缓存收益是乐观演示，实际必须加回写入/失效成本，也不能把供应商缓存命中折扣直接当成整个网站的节省比例。

在「首次写入系数 w、后续读取系数 r、相同前缀共用 R 次」的简化模型中，平均输入成本系数为 `[w+(R−1)r]/R`。例如假设 w=1.25、r=0.1，共用两次时系数为 0.675，节省 32.5%；只用一次反而增加 25%。是否有写入溢价、TTL、最低长度与命中规则必须按具体供应商和模型检查。

质量验收至少包含：任务成功率、事实与引用正确率、关键字段完整率、P50/P95 延迟、首 token 延迟、错误/重试率、每成功任务成本、客服投诉与付费转化/留存。上线前用固定分层测试集（普通、困难、长文档、异常和越权等）评估；上线后小流量对照和可回滚发布。所谓「降低模型成本 50%」若伴随重复提问或失败率增加，需要重算成功任务成本和客户留存。

## 7. API 与自部署：不存在通用 token 临界值

对同一任务分布、效果、延迟与可用性要求，分别比较：

`API 月成本 = F_api + Q × v_api`

`自部署月成本 = F_self + Q × v_self`

`盈亏临界任务数 Q* = (F_self−F_api)/(v_api−v_self)`，仅在分母大于零且满足容量、质量与服务条件时成立。

F_self 应含 GPU/主机或折旧、CPU/RAM、存储、网络基线、编排、监控、值班、冗余、模型升级与评估、安全、迁移的一次性成本管理摊销。云端整月租 GPU 时，计算费已在固定成本里，不能再把同一费用重复放入 v_self。自购机器还要计入电力、制冷、机位、保修与故障替换。共享应用数据库等两方案都相同的成本可以在差额比较时抵消，但完整账单仍需展示。

**纯假设示例**：自部署相对 API 额外固定成本 12,000 元/月，API 每任务成本 0.030 元、自部署边际成本 0.006 元，则名义临界值为 500,000 任务/月。假设两份推理副本在代表性输入/输出组合、满足延迟要求的实测输出吞吐各为 100 token/s，按 35% 可持续负载系数、30 天、平均输出 500 token 估算，月容量约 `2×100×0.35×2,592,000/500=362,880` 任务。名义临界量超过可用容量，需要新增资源后重新计算；如果还要保证单副本故障时服务不降级，可承诺容量还会更低。该例仅解释方法，不代表某款 GPU 或模型真实性能。

自部署必须实测代表性上下文长度、并发和输出长度；只看 GPU 峰值算力或单用户 token/s 不能推出生产成本。低平均利用率、明显流量峰谷、频繁更换模型、缺少推理运维能力时，API 的弹性往往更有价值。需求稳定且可用自有/开源模型满足质量要求时，再评估自部署；也可以固定基础负载自部署、峰值和难题交给 API，但混合方案增加路由、评估与运维复杂度。

## 8. 首月应得到的运营仪表盘与预算制度

- 按功能和套餐：MAU、任务数、成功率、实际每成功任务成本、付费率、免费补贴、贡献利润。
- 按供应商和系统：输入/缓存写/缓存读/输出 token、工具条数、媒体时长、流量、存储增长、CPU/GPU 利用率与账单差异。
- 按风险：日消耗与月预计、异常账户、最大单任务成本、P95/P99 任务成本、重试/循环次数、队列长度与峰值并发。
- 预算可设置分级提醒、功能降级、限制高成本功能和停止新任务等动作；百分比阈值依据业务自定。已在途请求的最坏花费要预留安全余量。
- 供应商预算提醒不能直接当成硬限额；应用必须具备自己的计量与限流，逐项确认供应商的停止机制、延迟和覆盖范围。

最后应提供「基准、低用量、高用量/压力」三个情景，并对至少五个变量做敏感性分析：每 MAU 任务数、每任务输出长度、高级模型占比、Agent 调用放大系数、付费转化率。另测峰值 x 倍、供应商涨价、缓存命中下降、被滥用和出网增加等压力情景。不要用单点成本数字承诺未来预算。

## 9. 官方来源：供主文档按需引用

1. AWS EC2 按需定价说明列出了计算之外的数据传输收费并说明报价税费口径，可支持「不能只看实例价格」的判断。区域与服务例外需以实际部署核对：[Amazon EC2 On-Demand Pricing](https://aws.amazon.com/ec2/pricing/on-demand/)。
2. Vercel 文档说明预算金额本身不会自动停止用量，需要启用相应的暂停功能，暂停后生产访问会受影响；不要把普通提醒等同硬预算：[Vercel Spend Management](https://vercel.com/docs/spend-management)。
3. Claude 官方 prompt caching 文档说明了静态前缀、缓存读写与具体模型规则；折扣和写入倍率随模型与设置不同，本文只提供计算方法：[Claude Prompt Caching](https://platform.claude.com/docs/en/build-with-claude/prompt-caching)。

以上均于 2026-09-13 检索。本文没有援引招聘统计、服务商外包报价或 GPU 具体性能报价；这些输入需由项目真实数据填充。
